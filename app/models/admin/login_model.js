exports.login = function (db, params, res) {
     res(null,true);
    // Get the documents collection

    /* var collection = db.collection('admin');
    
    collection.find(params).toArray(function (err, docs) {
        if (err) {
            throw err;
        } else {
            if (docs.length > 0) {
      
	           res(err, docs[0]);
	  
            } else {
                   
               
            }

        }
    }); */
}
